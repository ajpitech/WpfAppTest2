<UserControl x:Class="WpfAppTest2.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAppTest2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel >
        <StackPanel HorizontalAlignment="Left" Width="200" Height="250" Orientation="Vertical" Background="LightBlue">

            <Label HorizontalAlignment="Center" >This Is Home View</Label>
            <Button Command="{Binding HomeOption1View}" Width="150" Margin="15">HomeOption1</Button>
            <Button Command="{Binding HomeOption2View}" Width="150" Margin="15">HomeOption2</Button>
            <Button Command="{Binding HomeOption3View}" Width="150" Margin="15">HomeOption3</Button>
        </StackPanel>
        <!--<StackPanel HorizontalAlignment="Center" Width="200" Height="250" Orientation="Vertical">
            <ContentPresenter Content="{Binding ActiveViewHome}"></ContentPresenter>
        </StackPanel>-->

    </StackPanel>
</UserControl>